import React from 'react';
import { Screen } from '../ui/Screen';
import { Body, Title } from '../ui/Typography';

export function AlertsScreen() {
  return (
    <Screen>
      <Title>Alerts</Title>
      <Body>Rules + recent events (placeholder)</Body>
    </Screen>
  );
}
